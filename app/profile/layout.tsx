export const metadata = {
  title: 'Profile - Smart City Lab',
  description: 'Admin Profile',
};

export default function ProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
